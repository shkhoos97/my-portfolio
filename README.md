# my-portfolio

## AgroAI Telegram Bot
The Telegram bot lives in `telegram-bot/`.

Quick start:
```bash
cd telegram-bot
cp .env.example .env
# edit .env with TELEGRAM_BOT_TOKEN and OPENAI_API_KEY
npm install
npm run start
```

Full instructions: `telegram-bot/README.md`.
